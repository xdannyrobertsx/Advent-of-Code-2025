{
  "tasks": {
    "main": "deno lint && deno run --allow-read main.ts",
    "test": "deno test --no-check --allow-read --watch"
  },
  "imports": {
    "@std/expect": "jsr:@std/expect@1"
  },
  "lock": false
}
